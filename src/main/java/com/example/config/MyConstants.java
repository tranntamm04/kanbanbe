package com.example.config;

/**
 * Kept only to avoid breaking older imports during migration.
 * Runtime configuration now comes from application properties and environment variables.
 */
@Deprecated(forRemoval = true)
public final class MyConstants {

    private MyConstants() {
    }
}
